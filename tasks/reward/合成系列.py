# tasks/reward/hechengxilie.py

from core.utils import 缩放区域,是否有重叠,区域列表是否雷同

from tasks.reward.base import 强化奖励任务基类
from core.debug import 调试器
import time,random

class 合成系列强化(强化奖励任务基类):
    合成配置 = {
        "神器": {
            "标签": "神器",
            "检索页数": 1,
            "子标签": {
                "元神": {"关键词": "元神", "是否启用": True},
                "官印": {"关键词": "官印", "是否启用": True},
                "斗笠": {"关键词": "斗笠", "是否启用": True},
                "披风": {"关键词": "披风", "是否启用": True},
                "盾牌": {"关键词": "盾牌", "是否启用": True},
                "面甲": {"关键词": "面甲", "是否启用": True},
            }
        },
        "八卦": {
            "标签": "八卦",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },
        "圣魂": {
            "标签": "圣魂",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },
        "五行": {
            "标签": "五行",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },
        "守护": {
            "标签": "守护",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },
        "圣兽": {
            "标签": "圣兽",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },
        "修罗": {
            "标签": "修罗",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },
        "飞升": {
            "标签": "飞升",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },
       
        "神装": {
            "标签": "神装",
            "检索页数": 1,
            "子标签": {
                "神剑": {"关键词": "神剑", "是否启用": True},
                "神甲": {"关键词": "神甲", "是否启用": True},
                "梦幻仙莲": {"关键词": "梦幻", "是否启用": True},               
            }
        },
        "魂骨": {
            "标签": "魂骨",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },
         "兽神": {
            "标签": "兽神",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },      
         "暗殿": {
            "标签": "暗殿",
            "检索页数": 1,
            "合成所有": True,
            "子标签": {          
            }
        },
    }
    任务ID = "hechengxilie"
    任务名称 = "合成系列强化"  
    是否启用: bool = True  
    最小间隔秒 = 3000
    最大间隔秒 = 6000

    # 子标签启用属性
    元神 = True
    官印 = True
    斗笠 = True
    披风 = True
    盾牌 = True
    面甲 = True
    神剑 = True
    神甲 = True
    梦幻仙莲 = True

    # 合成所有属性
    八卦 = True
    圣魂 = True
    五行 = True
    守护 = True
    圣兽 = True
    修罗 = True
    飞升 = True
    魂骨 = True
    兽神 = True
    暗殿 = True
    def 同步到合成配置(self):
        """将属性同步到合成配置字典"""
        c = self.合成配置

        # 神器子标签
        c["神器"]["子标签"]["元神"]["是否启用"] = self.元神
        c["神器"]["子标签"]["官印"]["是否启用"] = self.官印
        c["神器"]["子标签"]["斗笠"]["是否启用"] = self.斗笠
        c["神器"]["子标签"]["披风"]["是否启用"] = self.披风
        c["神器"]["子标签"]["盾牌"]["是否启用"] = self.盾牌
        c["神器"]["子标签"]["面甲"]["是否启用"] = self.面甲

        # 神装子标签
        c["神装"]["子标签"]["神剑"]["是否启用"] = self.神剑
        c["神装"]["子标签"]["神甲"]["是否启用"] = self.神甲
        c["神装"]["子标签"]["梦幻仙莲"]["是否启用"] = self.梦幻仙莲

        # 合成所有
        c["八卦"]["合成所有"] = self.八卦
        c["圣魂"]["合成所有"] = self.圣魂
        c["五行"]["合成所有"] = self.五行
        c["守护"]["合成所有"] = self.守护
        c["圣兽"]["合成所有"] = self.圣兽
        c["修罗"]["合成所有"] = self.修罗
        c["飞升"]["合成所有"] = self.飞升
        c["魂骨"]["合成所有"] = self.魂骨
        c["兽神"]["合成所有"] = self.兽神
        c["暗殿"]["合成所有"] = self.暗殿
    def 执行(self) -> bool:
        调试器.debug("强化奖励", "合成系列强化: 开始检查")               
        if not self.页面.合成强化.进入小菜单带红点(self.配置.区域.主界面.合成按钮带红点区域标签.元组,
                                        "合|成"
                            ):            
            self.下次执行时间 = time.time() + random.randint(180, 600)
            调试器.debug("强化奖励", "合成系列强化: 进入合成失败")
            return False
 
        self._执行合成()      
        return True
   
    def _执行合成(self) -> bool:
        """合成主流程"""
        是否产生合成 = False
        已处理标签 = set()
        最大标签页数 = 3

        time.sleep(max(0.5, self.配置.刷新等待秒))
        self.页面.线程.刷新截图()

        标签文字信息 = self.辅助识别器.获取区域文字坐标(self.配置.区域.合成.合成左分类卡区域标签.元组)
        if 标签文字信息 is None:
            return False

        for _ in range(最大标签页数):
            for 标签名称, 标签配置 in self.合成配置.items():
                if 标签名称 in 已处理标签:
                    continue

                标签区域 = 标签文字信息.find(标签配置["标签"])
                if 标签区域 is None:
                    continue

                if not self._进入合成标签页(标签区域):
                    continue

                已处理标签.add(标签名称)

                # 处理子标签（含翻页）
                for 页码 in range(标签配置.get("检索页数", 1)):
                    if 页码 > 0:
                        self.子标签翻页()
                        time.sleep(self.配置.默认等待秒)
                        self.页面.线程.刷新截图()
                    if 标签配置.get("合成所有", False):

                        是否产生合成=self._处理合成所有子标签()

                    else:
                        子标签文字信息 = self.辅助识别器.获取区域文字坐标(
                            self.配置.区域.合成.合成中分类卡区域标签.元组
                        )
                        if 子标签文字信息 is None:
                            continue

                        for 子名称, 子配置 in 标签配置["子标签"].items():
                            if self._处理合成子标签(子标签文字信息, 子名称, 子配置):
                                是否产生合成 = True

            if len(已处理标签) >= len(self.合成配置):
                break

            # 翻页标签
            self.标签翻页()
            time.sleep(self.配置.默认等待秒)
            self.页面.线程.刷新截图()

            新标签文字信息 = self.辅助识别器.获取区域文字坐标(self.配置.区域.合成.合成左分类卡区域标签.元组)
            if 新标签文字信息 is None:
                break

            有新标签 = False
            for 标签名称 in self.合成配置:
                if 标签名称 not in 已处理标签:
                    if 新标签文字信息.find(标签名称) is not None:
                        有新标签 = True
                        break

            if not 有新标签:
                break

            标签文字信息 = 新标签文字信息

        return 是否产生合成
    
    def 标签翻页(self):
        x,y=self.配置.区域.合成.合成左分类卡区域标签.随机点(0.5)
        self.动作.drag(x,y,x,y-350)
    def 子标签翻页(self):
        x,y=self.配置.区域.合成.合成中分类卡区域标签.随机点(0.5)
        self.动作.drag(x,y,x,y-280)

    def _进入合成标签页(self, 标签区域) -> bool:
        """点击进入合成标签页"""
        self.通用操作.点击区域(标签区域, 2)
        time.sleep(self.配置.默认等待秒)
        self.页面.线程.刷新截图()
        return True


    def _处理合成所有子标签(self) -> bool:
        """处理单个合成标签，返回是否执行了合成"""
        合成次数 = 0
        for _ in range(10):
            if not self.页面.创建寻图偏移点击操作(
                self.配置.区域.合成.合成中分类卡区域标签.元组,
                "红点1.bmp",
                (-60,5,-10,20),           
            ).执行():
                break
            time.sleep(self.配置.默认等待秒)
            self.页面.线程.刷新截图()            
            for _ in range(10):
                if not self.页面.创建寻图点击原区域操作(
                    self.配置.区域.合成.合成打造含红点区域标签.元组,
                    "红点1.bmp",
                    0.3
                ).执行():
                    break
                合成次数 += 1
                time.sleep(self.配置.默认等待秒*2)
                self.页面.线程.刷新截图()
        return 合成次数 > 0

            

    def _处理合成子标签(self, 子标签文字信息, 子名称: str, 子配置: dict) -> bool:
        """处理单个合成子标签，返回是否执行了合成"""
        if not 子配置["是否启用"]:
            return False

        子标签区域 = 子标签文字信息.find(子配置["关键词"])
        if 子标签区域 is None:
            return False

        原始红点列表 = self.辅助识别器.查找图片多结果(
            self.配置.区域.合成.合成中分类卡区域标签.元组, "红点1.bmp"
        )
        if 原始红点列表 is None:
            return False

        if not 是否有重叠(子标签区域, 原始红点列表):
            return False

        self.通用操作.点击区域(子标签区域)
        time.sleep(self.配置.默认等待秒)
        self.页面.线程.刷新截图()

        点击后红点列表 = self.辅助识别器.查找图片多结果(
            self.配置.区域.合成.合成中分类卡区域标签.元组, "红点1.bmp"
        )
        if 点击后红点列表 is None:
            return False
        if 区域列表是否雷同(原始红点列表, 点击后红点列表):
            return False

        合成次数 = 0
        for _ in range(10):
            if not self.页面.创建寻图点击原区域操作(
                self.配置.区域.合成.合成打造含红点区域标签.元组,
                "红点1.bmp",
                0.3
            ).执行():
                break
            合成次数 += 1
            time.sleep(self.配置.默认等待秒*2)
            self.页面.线程.刷新截图()

        调试器.info("合成", f"{子名称}: 完成 {合成次数} 次合成")
        return 合成次数 > 0
           

         

      
